/**
 * Compatibility exports for the canonical Wishcraft token contract.
 * New code should import from src/config/types.ts.
 */

export type { ColorValue, WishcraftTokens } from "../../config/types.ts";
